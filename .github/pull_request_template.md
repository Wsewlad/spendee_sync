## Summary

<!-- What does this PR change, and why? -->

## Test plan

<!-- How was this verified? e.g. `pytest`, `flake8 spendee_sync/ tests/`, `black --check spendee_sync/ tests/`, manual run of `python -m spendee_sync.task` -->

- [ ]
